export { SessionTreeProvider } from './treeProvider';
export { deepSearch } from './sessionSearch';
export type { SearchResult } from './types';
export type { WorkspaceInfo, SessionMeta, AgentMeta, WebviewWorkspace, WebviewSession, WebviewAgent } from './types';
